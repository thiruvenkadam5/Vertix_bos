name: Jekyll site CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: 
            - self hosted
             - label-1

    steps:
    - uses: actions/checkout@v4
    
