    name: Deploy to Server

    on:
      push:
        branches:
          - main

    jobs:
      deploy:
        runs-on: self-hosted
        steps:
          - name: Checkout code
            uses: actions/checkout@v3

          - name: Run deployment
            run: |
              # Replace with your deployment commands
              echo "My secret key is: ${{ secrets.SSH_PRIVATE_KEY }}"
               
